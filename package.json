{
  "name": "seqshelf",
  "displayName": "SeqShelf — 4-Layer Sequence Diagram Shelf",
  "description": "Mermaid シーケンス図を L1運用 / L2システム構成 / L3クラス間 / L4関数内部 の4階層に束ね、本棚のように取り出して読み・GUIで編集するWebアプリ。",
  "version": "0.2.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import=tsx test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "mermaid": "^11.4.0"
  }
}
